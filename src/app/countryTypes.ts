// tslint:disable-next-line: interface-over-type-literal
export type Country = {
    code: string
    name: string
    native: string
    phone: string
    currency: string
    emoji: string
    emojiU: string
}